# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.local
*.tsbuildinfo
# tsc -b (run as part of `npm run build`) compiles vite.config.ts as a side
# effect of building the tsconfig.node.json project reference
/vite.config.js
/vite.config.d.ts

# editor/OS
.DS_Store
Thumbs.db

# tool-local config
.claude/

# raw working assets (source material for the pipeline; the site serves
# the processed copies from public/assets instead)
/assets/
/blender/
/musica/
/icons_32x32/
/fotoprogettiRiassunto/
/base.gif
/hero.png

# stray scratch files
Users*scratchpad*
/Th/
